# ARIA Backend — Python dependencies (Build Plan A2)
# Pin minor versions; only upgrade via PRs that run the eval suite.
# Day 1: web framework + reliability + supabase. Feature-specific deps land
# as each feature ships, to keep the HF Spaces image slim.

# --- Web framework ---
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.17

# --- Validation + settings ---
pydantic==2.9.2
pydantic-settings==2.6.1

# --- HTTP client ---
httpx==0.27.2

# --- Structured logging ---
structlog==24.4.0

# --- Reliability (retries + circuit breakers) — A2 §11 ---
tenacity==9.0.0
pybreaker==1.2.0

# --- Rate limiting ---
slowapi==0.1.9

# --- Supabase (DB + Auth + Storage) ---
supabase==2.9.1

# --- PDF + OCR (Week 1 Day 5) ---
pdfplumber==0.11.4         # MIT, replaces PyMuPDF (AGPL)
pytesseract==0.3.13
Pillow==10.4.0

# --- Embeddings + reranker (Week 1 Day 6) ---
# Loads bge-m3 (1024-dim) and bge-reranker-v2-m3 at runtime via HF cache
sentence-transformers==3.3.1

# --- LLM (Week 2 Day 8) ---
groq==0.13.0

# --- TTS (Week 3 Day 15) ---
edge-tts==6.1.20
piper-tts==1.2.0

# Eval (Week 4): ragas, sentry-sdk, posthog — added when wired
