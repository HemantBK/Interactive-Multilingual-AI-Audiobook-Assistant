# ARIA backend — Python dependencies (build plan)
# Pin minor versions; only upgrade via PRs that run the eval suite.
# Day 1: web framework + reliability + supabase. Feature-specific deps land
# as each feature ships, to keep the HF Spaces image slim.
#
# Day 27: CVE-driven bumps (fastapi/starlette, pillow, python-multipart,
# pdfplumber→pdfminer.six, sentence-transformers→transformers,
# langchain-text-splitters). See CHANGELOG.

# --- Web framework ---
fastapi==0.120.4
# starlette is a fastapi transitive — explicit pin guards the GHSA-2c2j-9gv5-cj73
# and GHSA-7f5h-v6xp-fcq8 CVEs (fixed in 0.49.1).
starlette>=0.49.1,<0.50.0
uvicorn[standard]==0.32.0
python-multipart==0.0.26

# --- Validation + settings ---
pydantic==2.9.2
pydantic-settings==2.6.1

# --- HTTP client ---
httpx==0.27.2

# --- Structured logging ---
structlog==24.4.0

# --- Reliability (retries + circuit breakers) — build plan §11 ---
tenacity==9.1.2
pybreaker==1.2.0

# --- Rate limiting ---
slowapi==0.1.9

# --- Supabase (DB + Auth + Storage) ---
supabase==2.9.1

# --- PDF + OCR (Week 1 Day 5) ---
pdfplumber==0.11.9         # MIT, replaces PyMuPDF (AGPL); pulls pdfminer.six 20251230 (CVE fixes)
pytesseract==0.3.13
Pillow==12.2.0             # CVE fixes (GHSA-cfh3-3jmp-rvhc, GHSA-whj4-6x5x-4v2j)

# --- Language detection (Week 1 Day 5) ---
langdetect==1.0.9

# --- Embeddings + reranker (Week 1 Day 6) ---
# Loads bge-m3 (1024-dim) and bge-reranker-v2-m3 at runtime via HF cache
sentence-transformers==5.4.1
# transformers is a sentence-transformers transitive — explicit pin guards
# the GHSA-69w3-r845-3855 CVE (fixed in 5.0.0).
transformers>=5.0.0,<6.0.0

# --- Chunking (Week 1 Day 6) ---
langchain-text-splitters==1.1.2

# --- LLM (Week 2 Day 8) ---
groq==0.13.0

# --- TTS (Week 3 Day 15) ---
edge-tts==6.1.19
piper-tts==1.2.0

# --- Observability (Week 4 Day 22) ---
sentry-sdk[fastapi]==2.18.0
posthog==3.7.0

# Eval (Week 4 Day 23): ragas — added when wired
